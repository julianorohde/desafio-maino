<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .container {
      width: 100%;
      max-width: 400px;
      padding: 20px;
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      box-sizing: border-box;
      text-align: center;
    }

    h2 {
      color: #333333;
      margin-bottom: 20px;
    }

    .field {
      margin-bottom: 15px;
      text-align: left;
    }

    .field label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
      color: #555555;
    }

    .field input {
      width: 100%;
      padding: 10px;
      border: 1px solid #dddddd;
      border-radius: 4px;
      box-sizing: border-box;
    }

    .actions {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
    }

    .btn-custom {
      background-color: #4CAF50; /* Cor de fundo verde */
      color: white; /* Cor do texto */
      padding: 10px 24px; /* Espaçamento interno */
      border: none; /* Sem borda */
      border-radius: 5px; /* Bordas arredondadas */
      text-align: center; /* Alinhamento do texto */
      font-size: 16px; /* Tamanho da fonte */
      cursor: pointer; /* Cursor em formato de mão */
      width: 100%; /* Largura completa do botão */
      box-sizing: border-box; /* Inclui o padding no cálculo da largura */
      margin: 0 auto; /* Centraliza o botão */
      display: block;
    }

    .btn-custom:hover {
      background-color: #45a049;
    }

    .button-group {
      display: flex;
      flex-direction: row; /* Coloca os botões lado a lado */
      gap: 10px; /* Espaçamento entre os botões */
      margin-top: 20px; /* Espaçamento acima do grupo de botões */
    }

    .button-group .btn-custom {
      flex: 1; /* Faz os botões ocuparem o mesmo espaço */
      width: 100%; /* Faz os botões ocuparem a largura disponível */
      padding: 10px 0; /* Ajuste o padding para manter o tamanho uniforme */
      font-size: 16px; /* Tamanho da fonte igual ao do botão de login */
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Forgot your password?</h2>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <div class="actions">
        <%= f.submit "Send me reset password instructions", class: "btn-custom" %>
      </div>
    <% end %>

    <div class="button-group">
      <%= link_to 'Log In', new_session_path(resource_name), class: "btn-custom" %>
      <%= link_to 'Sign Up', new_registration_path(resource_name), class: "btn-custom" %>
    </div>
  </div>
</body>
</html>
